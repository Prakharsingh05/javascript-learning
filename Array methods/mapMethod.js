//map methods

const numbers = [1,2,3,4,5];



const square =function (number){

    return number * number ; // must return something if you want to use map method
}
const squareNumber = numbers.map(square); 